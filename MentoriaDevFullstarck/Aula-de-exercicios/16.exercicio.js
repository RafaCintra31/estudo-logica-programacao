/*
        Você está criando um sistema que permite palavras entre 4 e 8 caracteres. 
   - Crie um algoritmo que recebe um texto e faça a contagem de palavras válidas e 
   - as inválidas. 
*/

function quantidadeDeAlgoritimo(palavra = ''){

        //for(let algoritmo = 4; algoritmo <= 8; algoritmo++){
        //        for(let i = 0; i <= palavra.length; i++){
        //                return (palavra != algoritmo) ? 'Válido' : 'inválido'
        //        }
        //}

       //for(let i = 0; i > palavra.length;i++){
       //        return (palavra && 4 || 8) ? 'valido' : 'invalido'
       //        //for(let algorismo = 4; algorismo < 8; algorismo++){
       //        //        return (algorismo != palavra) ? 'valido' : 'invalido'
       //        //}
       //}
}


let resultado = quantidadeDeAlgoritimo('um')
console.log(resultado);

